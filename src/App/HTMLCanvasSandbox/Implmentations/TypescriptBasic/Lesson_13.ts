
export class Lesson {
    constructor() {
        console.log('Lesson ======================== dictionary');
        // your code goes here
        

        
    // // https://docs.microsoft.com/en-us/dotnet/api/system.collections.generic.dictionary-2?view=net-5.0
    // // using System.Collections.Generic;

    // Dictionary<string, int> dictionary = new Dictionary<string, int>();

    // dictionary.Add("nj", 3);
    // dictionary.Add("dj", 2);
    // dictionary.Add("mj", 5);


    // if (dictionary.ContainsKey("mj").Equals(true)) {
    //   Print(dictionary["mj"].ToString());
    // }


    // foreach (string key  in dictionary.Keys) {
    //   Print("Key = {0}, Value = {1}", key, dictionary[key].ToString());
    // }


    // foreach (KeyValuePair<string, int> d  in dictionary) {
    //   Print("Key = {0}, Value = {1}", d.Key, d.Value);
    // }



    
    // // https://docs.microsoft.com/en-us/dotnet/api/system.collections.hashtable?view=net-5.0
    // // using System.Collections;

    // Hashtable hashtable = new Hashtable();

    // hashtable.Add("nj", 1);
    // hashtable.Add("dj", "3.5");
    // hashtable.Add("mj", new Car());

    // if (hashtable.ContainsKey("mj").Equals(true)){
    //   var temp = hashtable["mj"];
    //   Print(temp.ToString());
    // }

    // foreach (string NowKey in hashtable.Keys){
    //   var temp = hashtable[NowKey];
    //   Print(temp.ToString());
    // }


    // foreach( DictionaryEntry de in hashtable ){
    //   Print("Key = {0}, Value = {1}", de.Key, de.Value);
    // }


    

    }
}